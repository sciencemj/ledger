import {Text, View} from "react-native";

export default function ReceiptModal({ navigation }) {
    return(
        <View className={'items-center justify-center flex-1'}>
            <Text>modal</Text>
        </View>
    )
}